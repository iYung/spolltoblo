# Lobby Actions Checklist

- [x] Task A — `src/components/Room.jsx` — Replace `recentCards` state with `lobbyActions`. Change `const [recentCards, setRecentCards] = useState([])` (line 35) to `const [lobbyActions, setLobbyActions] = useState([])`. Update all three `setRecentCards` calls (lines 246, 346, 354) to push `{ type: 'card', card, playerName }` objects using `setLobbyActions`. Update the CardSidebar prop at line 511 from `recentCards={recentCards}` to `lobbyActions={lobbyActions}`.

- [x] Task B — `src/components/Room.jsx` — Add `rollD20` function and `d20-roll` WebSocket handler. After `broadcastGameEvent` (line 96), add: `function rollD20() { const result = Math.ceil(Math.random() * 20); broadcastGameEvent({ type: 'd20-roll', result, playerName }); setLobbyActions((prev) => [{ type: 'roll', result, playerName }, ...prev]) }`. In the `game-event` WebSocket handler (after the existing `card-pinned` else-if, line 249), add an `else if (payload.type === 'd20-roll')` branch that calls `setLobbyActions((prev) => [{ type: 'roll', result: payload.result, playerName: payload.playerName }, ...prev])`. Pass `onRollD20={rollD20}` to CardSidebar. **Depends on Task A being done first** (same file, uses `setLobbyActions`).

- [x] Task C — `src/components/CardSidebar.jsx` — Update the sidebar to use the new `lobbyActions` prop and render the unified feed. Replace `recentCards = []` in the props destructure with `lobbyActions = []` and add `onRollD20`. Rename the section condition from `recentCards.length > 0` to `lobbyActions.length > 0`. Change the header text from "Recently played by others" to "Lobby Actions". Above the section header, add a Roll d20 button: `<button className="d20-btn" onClick={onRollD20}>Roll d20</button>`. Map over `lobbyActions` instead of `recentCards`: render `type === 'card'` entries with the existing draggable card layout; render `type === 'roll'` entries as `<div className="action-roll-entry"><span className="recent-player">{entry.playerName}</span> rolled a <strong>{entry.result}</strong></div>`. Show the d20 button always (not gated on feed length) — move it just above the `lobbyActions.length > 0` section.

- [x] Task D — `src/index.css` — Add styles for the d20 button and roll feed entries. `.d20-btn`: style consistent with sidebar buttons — small, full-width or inline, subtle border, hover effect; add `margin` to separate it from the feed. `.action-roll-entry`: no drag hint, distinct from card entries — `padding: 6px 8px`, maybe a slightly different background or left-border accent to distinguish it from card play rows.
