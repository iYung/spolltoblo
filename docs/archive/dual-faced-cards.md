# Dual-Faced Cards Checklist

- [x] Task A — `src/utils/cardImages.js` — Create a `cardImages(card)` helper that returns an array of image URLs: `[normal]` for single-faced cards (`card.image_uris?.normal`), `[face0, face1]` for dual-faced cards (`card.card_faces` where each face has `image_uris?.normal`). Export as named export. This is the only task others depend on; complete it first.

- [x] Task B — `src/components/CardSidebar.jsx` — Replace the local `cardImage` helper with an import of `cardImages` from `../utils/cardImages.js`. Keep `cardImage` (the single-URL version) only as a local guard function for the `hoveredCard &&` check, or replace the guard with `cardImages(hoveredCard).length > 0`. In the hover popup (lines 162–175), replace the single `<img>` with a `cardImages(hoveredCard).map(...)` that renders each image at `width: 300` with `borderRadius: 8` and `display: 'block'`. Wrap the images in a flex container with `display: 'flex'` and `gap: 8` so two faces appear side by side. The outer popup `<div>` already has `lineHeight: 0`; no other positioning changes needed.

- [x] Task C — `src/components/CardPin.jsx` — Replace the local `cardImage` helper with an import of `cardImages` from `../utils/cardImages.js`. In the hover popup (lines 70–74), replace `<img src={img} ...>` with a flex container (`display: 'flex'`, `gap: 8`) that maps over `cardImages(pin.card)` and renders each as an `<img className="card-popup-img">`. Update the guard on line 70 from `img &&` to `cardImages(pin.card).length > 0 &&` and remove the now-unused `const img = cardImage(pin.card)` line (line 13).

- [x] Task D — `src/components/PlayerVideo.jsx` — Replace the local `cardImage` helper with an import of `cardImages` from `../utils/cardImages.js`. In the commander hover popup (lines 194–214), the inner `commanders.map` currently calls `cardImage(cmdr)` and renders one `<img>` per commander. Replace it so each commander contributes `cardImages(cmdr)` images — i.e., flat-map across commanders to get all image URLs, then render each at the appropriate width. Keep the existing width logic: if the total image count is greater than 1, use `180` width per image; otherwise `300`. Update the outer guard (`commanders.some(c => cardImage(c))`) to use `commanders.some(c => cardImages(c).length > 0)`.

- [x] Task E — `src/components/CommanderPicker.jsx` — Replace the local `cardImage` helper with an import of `cardImages` from `../utils/cardImages.js`. In the hover popup (lines 123–136), replace the single `<img src={cardImage(hoveredCard)} ...>` with a flex container (`display: 'flex'`, `gap: 8`) that maps over `cardImages(hoveredCard)` and renders each as an `<img>` at `width: 240` with `borderRadius: 8` and `display: 'block'`. Update the guard `cardImage(hoveredCard) &&` to `cardImages(hoveredCard).length > 0 &&`.
